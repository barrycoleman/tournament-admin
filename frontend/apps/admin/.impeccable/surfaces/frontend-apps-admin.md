---
version: 1
slug: "frontend-apps-admin"
primary_target: "frontend/apps/admin"
related_targets: []
---

## Direction contract

THESIS: This admin tool is the meet-day operations desk, not a broadcast scoreboard — it refuses the flashy jumbotron/LED-scoreboard default every "competition software" brief reaches for. The screen should feel like the one trustworthy laminated clipboard at the check-in table: plain, dense, and completely legible under gym lighting.

OWN-WORLD: Warm off-white paper ground, near-black ink text, one accent — a marker/highlighter amber used only for primary actions and live/active state, never decoration. One system-stack sans throughout, tabular numerals for data. Hairline 1px dividers, small consistent 4-6px radius (never zero, never bubbly), no gradients, no shadows beyond a 1px hairline lift on floating panels. Raised from two declined challengers: strict single-accent restraint and printed, no-radius-leaning state marks (from the ruling-engine reference-setting card); tabular grid-precise number columns for rosters/schedules (from the data-matics card).

STORY: An organizer scans a dense list, finds the one thing needing action, acts, and trusts the result saved — nothing else competes for attention.

FIRST VIEWPORT: App shell — thin header (product name left, theme toggle + logout right), a persistent left-rail nav below it, content panel filling the rest with generous internal padding and a single-column form or dense table depending on the route.

FORM: Own grounded direction, assigned index 7 of 7 ("meet-day ops ledger"), seed key 54d7aa44. No challenger won both axes for this dense CRUD Operate surface; all six declined for register mismatch (art-data, novelty-retro, print-only, decorative-gradient, or off-topic metaphor).

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
